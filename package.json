{
  "name": "plexsonic",
  "version": "1.0.0",
  "description": "Real-time music Now Playing display for Plex",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:all": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "NODE_ENV=production npm run start --workspace=backend",
    "clean": "rm -rf backend/dist frontend/dist"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
